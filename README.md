# Poker-Simulator

## Running the Program

To run the program you can either clone the repository or download the file and open your Haskell shell and go to the directory where you have stored the file.

Run the following line

`ghci HaskellPokerSimulator.hs`

Once this has loaded the file simply type `main` and the program will run
